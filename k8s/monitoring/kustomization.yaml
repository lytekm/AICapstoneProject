# Monitoring kustomization -- apply separately or include from base.
# Requires prometheus-operator CRDs (ServiceMonitor) to be installed.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - api-servicemonitor.yaml
  - vllm-servicemonitor.yaml
